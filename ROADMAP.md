# 프로젝트 로드맵 & 진행현황

이 문서는 2026-09-12에 세운 원본 7주 로드맵({프로젝트 로드맵 Artifact} 참고)과
`TASKS.md`에 기록된 실제 완료 내역을 대조한 것입니다. 매 세션 시작 시 이 문서를
먼저 확인하면 "원래 계획 대비 지금 어디쯤인지"를 바로 파악할 수 있습니다.

## 한 줄 요약 (2026-09-15 기준)

프로젝트 기간은 2026.09.12~10.31(7주) 중 **아직 1주차(W1) 안**이지만, 실제
구현 진도는 원래 W7(SW2 완성) 목표 대부분에 이미 도달했습니다. SW1은 사실상
완성, SW2도 핵심 기능(모델 레지스트리·시뮬레이터·통계비교)이 이미 동작 중이며,
원래 계획에 없던 확장(거래비용 모델링, 포지션 사이징, 거버넌스 승격 기준,
시장 레짐 필터, 뉴스 게이트, 대시보드)까지 추가로 완료했습니다. 남은 원래
완료기준은 **walk-forward 검증 1회 뿐**입니다.

## 주차별 계획 vs 실제 진행

| 주차 | 기간 | 원래 산출물 | 실제 상태 |
|---|---|---|---|
| W1 | 09.12–09.18 | GitHub repo, 개발환경, 토스/KIS API 신청, yfinance PoC | 🟡 부분완료 — repo(Public, 원래 계획은 private)·개발환경·yfinance PoC는 완료. 토스 API는 신청 안 함, KIS는 사용자 지시로 보류("나중에하자") |
| W2 | 09.19–09.25 | 지표 7종 + 유닛테스트 | ✅ 완료 — RSI·MACD·볼린저밴드·이동평균·거래량·PER·PBR (`sw1/indicators/`) |
| W3 | 09.26–10.02 | 뉴스 입력·전처리 + Anthropic API 감성점수화 | ✅ 완료, 현재 추가 고도화 진행 중 (누적 로그 + 종목무관 일반시황 반영) |
| W4 | 10.03–10.09 | 통합 스코어링 + 매매기준 산출 + M7 통합테스트 → SW1 완성 | ✅ 완료 (`sw1/scoring/integrate.py`, `sw1/criteria/generator.py`) |
| W5 | 10.10–10.16 | 모델 레지스트리, 모의투자 시뮬레이터, DB→Supabase 전환 | 🟡 완료(대체 설계) — 레지스트리·시뮬레이터(`sw2/models.py`, `sw2/ledger.py`)는 완료. Supabase 대신 **GitHub Actions가 결과를 git에 직접 커밋**하는 방식으로 영속성 확보 (아래 "달라진 결정" 참고) |
| W6 | 10.17–10.23 | 매일 자동실행 파이프라인, Daily 수익률 축적 | 🟡 완료(대체 설계) — Cowork 예약작업 대신 **GitHub Actions cron**(평일 22:00/22:30 UTC)으로 구현, 매일 축적 중 |
| W7 | 10.24–10.30 | walk-forward 검증, 통계적 유의성 비교 → SW2 완성 | 🟡 부분완료 — 통계비교는 원래 계획보다 더 엄밀하게 완료(t-test, Cohen's d, FDR/Bonferroni 다중비교 보정, block bootstrap p-value, 자기상관 경고). **walk-forward 검증은 아직 미착수 — 다음 작업** |
| BUF | 10.31 | 통합 점검, 문서화 | 예정대로 마지막에 진행 |

## 원래 계획에 없었지만 추가로 완료한 것

- 거래비용(수수료) 모델링, 리스크 기반 포지션 사이징
- 거버넌스/모델 승격 기준(`sw2/governance.py::evaluate_promotion`) — 단, 아직
  파이프라인/대시보드에 연결 안 됨(제품 결정 대기)
- 시장 레짐 필터(risk_on/risk_off), 실적 발표 블랙아웃
- 가격기준모델(price-criteria) 2종 + 뉴스 게이트, 대시보드 뉴스 노출, 뉴스
  입력 GitHub Issue Form 자동화

## 원래 계획과 달라진 아키텍처 결정 (확인 필요)

1. **저장소 공개범위**: 원래 계획은 private였는데 현재 Public 저장소입니다.
2. **DB**: Supabase 대신 GitHub Actions가 매 실행 결과를 CSV/JSON으로 git에
   직접 커밋하는 방식을 씁니다. 지금까지는 문제없이 동작하지만, 데이터량이
   늘어나면(예: 몇 달치 daily 히스토리) git 저장소 자체가 무거워질 수 있어
   나중에 재검토가 필요할 수 있습니다.
3. **자동실행 방식**: Cowork 예약작업 대신 GitHub Actions cron을 씁니다 —
   "실행마다 새 세션"이라는 원래 계획의 문제를 더 직접적으로 해결하는 방식이라
   오히려 원안보다 안정적이라고 판단했습니다.
4. **토스증권 API**: 신청하지 않았습니다. 대상이 국내주식이 아닌 미국 M7이라
   yfinance만으로 데이터가 충분히 커버되는 상태입니다 — 실제로 필요한지 재검토
   대상.

## 다음 우선순위

1. 뉴스 입력 고도화 (진행 중): 종목별 코멘터리 누적 로그화 + 종목무관 일반
   시황 입력/반영
2. Walk-forward 검증 최소 1회 실행 (원래 SW2 완료기준, 유일한 미착수 항목)
3. 그 외 코드베이스 자체 개선 여지 탐색 또는 사용자 새 지시 대기

## 참고

- 원본 로드맵 전체(아키텍처, API 신청 절차, Cowork 활용법, 리스크 목록)는
  [로드맵 Artifact](https://claude.ai/code/artifact/e764107a-9ca5-4716-9491-21eb1ac66653)에
  그대로 있습니다. 이 문서는 그 위에 "지금 어디까지 왔는지"만 얹은 요약입니다.
- 작업 단위 상세 이력(무엇을 왜 어떻게 했는지)은 `TASKS.md`를 참고하세요.
